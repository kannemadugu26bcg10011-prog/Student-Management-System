# Student-Management-System
Student Management System is a Python-based application designed to manage student records. It allows users to add, view, search, and delete student details using roll number, name, age, and marks. The system automatically calculates grades and provides class performance statistics. Student data is stored using JSON for easy access and persistence
